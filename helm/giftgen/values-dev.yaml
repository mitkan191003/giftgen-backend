api:
  image:
    # Deployment updates set this tag at release time.
    tag: managed-by-pipeline

worker:
  image:
    # Deployment updates set this tag at release time.
    tag: managed-by-pipeline
