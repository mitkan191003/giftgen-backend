api:
  image:
    # The delivery pipeline overrides this with the source commit SHA in Argo CD.
    tag: managed-by-pipeline

worker:
  image:
    # The delivery pipeline overrides this with the source commit SHA in Argo CD.
    tag: managed-by-pipeline
