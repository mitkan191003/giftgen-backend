api:
  image:
    # Production uses one release tag across API and worker.
    tag: managed-by-promotion

worker:
  image:
    # Production uses one release tag across API and worker.
    tag: managed-by-promotion
