api:
api:
  image:
    # Promote a tested source commit SHA into prod with the shared delivery deploy project.
    tag: managed-by-promotion

worker:
  image:
    # Keep worker and API on the same promoted commit SHA.
    tag: managed-by-promotion
